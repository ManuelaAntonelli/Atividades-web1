function addTask() {
    const taskText = document.getElementById('InputAtividade').value;
    const priorityClass = document.getElementById('priorityInput').value;
    const taskList = document.getElementById('taskList');

    if ("InputAtividade" === '') {
        alert("Por favor, digite uma tarefa!");
        return;
    }

    // Criando a coluna do grid
    function adicionar(){
        
    }
    
}